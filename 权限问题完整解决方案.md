# 权限问题完整解决方案

## 问题根本原因 🎯

### 1. 安装了错误的 APK 版本

**症状**：应用无法弹出权限请求对话框，设置中显示 "This app does not send notifications"

**根本原因**：安装的是 **System 版本** 而不是 **Normal 版本** 的 APK

从 `adb shell dumpsys package com.ai.voice` 可以看到，当前安装的应用 **缺少以下权限声明**：
- ❌ `android.permission.RECORD_AUDIO`
- ❌ `android.permission.POST_NOTIFICATIONS`
- ❌ `android.permission.ACCESS_FINE_LOCATION`
- ❌ `android.permission.ACCESS_COARSE_LOCATION`
- ❌ `android.permission.ACCESS_WIFI_STATE`
- ❌ `android.permission.ACCESS_NETWORK_STATE`

### 2. 两个版本的区别

#### Normal 版本（普通版本）
- 路径：`app/src/main/AndroidManifest.xml`
- 包含所有权限声明
- 需要用户授予权限
- **适用于普通安装**

#### System 版本（系统预装版本）
- 路径：`app/src/system/AndroidManifest.xml`
- 使用 `tools:node="remove"` 移除了关键权限声明
- 使用 `android.uid.system`，自动获得系统权限
- **仅适用于系统预装（EDLA 认证）**

```xml
<!-- System 版本的 AndroidManifest.xml -->
<uses-permission 
    android:name="android.permission.RECORD_AUDIO"
    tools:node="remove" />
    
<uses-permission 
    android:name="android.permission.POST_NOTIFICATIONS"
    tools:node="remove" />
```

### 3. run.sh 的查找逻辑问题

之前的 `run.sh` 有 fallback 逻辑，如果找不到指定的 APK，会查找任何 Debug APK，**包括 System 版本**。

## 完整解决方案 ✅

### 修改 1: 修复 run.sh - 明确只安装 Normal 版本

```bash
# 查找 normal flavor 的 Debug APK（排除 system 版本）
echo ""
echo "🔎 4. 查找 Normal Debug APK..."

# 方法1：优先使用 normal/debug 目录
apk_path="app/build/outputs/apk/normal/debug/VoiceAssistant-3.19.43-Debug.apk"

# 方法2：如果固定路径不存在，使用通配符查找
if [ ! -f "$apk_path" ]; then
    apk_path=$(ls app/build/outputs/apk/normal/debug/VoiceAssistant-*-Debug.apk 2>/dev/null | head -1)
fi

# 方法3：在整个构建目录中查找，但排除 system 版本
if [ -z "$apk_path" ] || [ ! -f "$apk_path" ]; then
    echo "⚠️  未找到 normal/debug 目录，尝试查找排除 System 版本的 APK..."
    apk_path=$(find app/build/outputs/apk -type f -name "VoiceAssistant-*-Debug.apk" ! -name "*-System-*" ! -path "*/system/*" | head -1)
fi

# 验证不是 System 版本
if echo "$apk_path" | grep -qi "system"; then
    echo "❌ 错误: 找到的是 System 版本 APK，而不是 Normal 版本"
    echo "💡 System 版本会移除关键权限声明，导致权限请求失败"
    exit 1
fi
```

### 修改 2: PermissionHelper - 区分关键权限和可选权限

```kotlin
/**
 * 获取关键权限列表（必须拥有才能运行的权限）
 * 只包括：录音、位置
 */
fun getCriticalPermissions(): Array<String> {
    return arrayOf(
        Manifest.permission.RECORD_AUDIO,  // 必需：语音识别
        Manifest.permission.ACCESS_FINE_LOCATION,  // 必需：设备激活
        Manifest.permission.ACCESS_COARSE_LOCATION
    )
}

/**
 * 检查权限是否为关键权限
 */
fun isCriticalPermission(permission: String): Boolean {
    return permission in listOf(
        Manifest.permission.RECORD_AUDIO,
        Manifest.permission.ACCESS_FINE_LOCATION,
        Manifest.permission.ACCESS_COARSE_LOCATION
    )
}

fun getPermissionDescription(permission: String): String {
    return when (permission) {
        Manifest.permission.RECORD_AUDIO -> 
            "📱 录音权限（必需）：用于语音识别和语音指令"
        Manifest.permission.POST_NOTIFICATIONS -> 
            "🔔 通知权限（可选）：用于显示语音助手服务状态"
        Manifest.permission.ACCESS_FINE_LOCATION -> 
            "📍 精确位置权限（必需）：用于获取MAC地址进行设备激活"
        ...
    }
}
```

### 修改 3: FloatingLauncherActivity - 只检查关键权限

```kotlin
override fun onRequestPermissionsResult(...) {
    val result = PermissionHelper.onRequestPermissionsResult(...)
    
    when (requestCode) {
        REQUEST_BASIC_PERMISSIONS -> {
            // 只检查关键权限（录音和位置）
            val criticalDenied = result.deniedPermissions.filter { permission ->
                PermissionHelper.isCriticalPermission(permission)
            }
            
            if (criticalDenied.isEmpty()) {
                // 关键权限已授予，可以继续
                Log.d(TAG, "✅ 关键权限已授予")
                
                // 可选权限被拒绝不影响运行
                val optionalDenied = result.deniedPermissions.filter { 
                    !PermissionHelper.isCriticalPermission(it)
                }
                if (optionalDenied.isNotEmpty()) {
                    Toast.makeText(this, 
                        "通知权限被拒绝，部分功能可能受限", 
                        Toast.LENGTH_SHORT).show()
                }
                
                currentStep = PermissionStep.CHECK_STORAGE
                checkNextPermission()
            } else {
                // 关键权限被拒绝，无法继续
                showPermissionDeniedDialog("关键权限", criticalDenied)
            }
        }
    }
}
```

## 权限分类 📋

### 关键权限（必需，缺少无法运行）

1. **RECORD_AUDIO** - 录音权限 📱
   - 用途：语音识别和语音指令
   - 缺少影响：**无法使用语音功能**

2. **ACCESS_FINE_LOCATION** - 精确位置权限 📍
   - 用途：获取设备MAC地址进行激活
   - 缺少影响：**无法完成设备激活**

3. **ACCESS_COARSE_LOCATION** - 大致位置权限 📍
   - 用途：获取设备MAC地址（备用）
   - 缺少影响：**无法完成设备激活**

### 可选权限（建议授予，但不影响核心功能）

1. **POST_NOTIFICATIONS** - 通知权限 🔔
   - 用途：显示语音助手服务状态
   - 缺少影响：无法显示通知，但**不影响语音功能**
   - 仅 Android 13+ 需要

### 特殊权限

1. **SYSTEM_ALERT_WINDOW** - 悬浮窗权限 🪟
   - 用途：显示悬浮球界面
   - 必需，但通过设置页面授予

## 使用指南 🚀

### 步骤 1: 卸载旧版本（如果之前安装过）

```bash
adb uninstall com.ai.voice
```

### 步骤 2: 构建并安装 Normal 版本

```bash
./run.sh
```

脚本会：
1. ✅ 构建 `assembleNormalDebug`（Normal 版本）
2. ✅ 查找 `app/build/outputs/apk/normal/debug/` 下的 APK
3. ✅ 验证不是 System 版本
4. ✅ 安装 APK
5. ✅ 启动应用

### 步骤 3: 授予权限

应用启动后会自动弹出权限请求：

1. **第一次请求**：录音、位置权限
   - ✅ **必须授予**，否则无法使用

2. **第二次请求**（Android 13+）：通知权限
   - 建议授予，但**拒绝也能用**

3. **悬浮窗权限**：需要在设置中手动开启
   - 应用会引导您到设置页面

### 步骤 4: 完成激活

权限授予后，应用会：
1. 检查激活状态
2. 如果未激活，跳转到激活管理界面
3. 输入激活码或通过MAC白名单激活

## 验证权限声明 🔍

### 检查已安装应用的权限

```bash
adb shell dumpsys package com.ai.voice | grep "requested permissions:" -A 20
```

**期望输出（Normal 版本）**：
```
requested permissions:
  android.permission.INTERNET
  android.permission.RECEIVE_BOOT_COMPLETED
  android.permission.RECORD_AUDIO                    ✅ 必需
  android.permission.POST_NOTIFICATIONS              ✅ 可选
  android.permission.ACCESS_WIFI_STATE
  android.permission.ACCESS_FINE_LOCATION            ✅ 必需
  android.permission.ACCESS_COARSE_LOCATION          ✅ 必需
  android.permission.FOREGROUND_SERVICE
  android.permission.FOREGROUND_SERVICE_MICROPHONE
  ...
```

### 检查 APK 文件名

```bash
find app/build/outputs/apk -name "*.apk" -type f
```

**正确的输出**：
```
app/build/outputs/apk/normal/debug/VoiceAssistant-3.19.43-Debug.apk  ✅
app/build/outputs/apk/system/debug/VoiceAssistant-3.19.43-System-Debug.apk
```

确保安装的是 `normal/debug/` 下的文件！

## 常见问题 ❓

### Q1: 为什么之前能弹出权限请求，现在不行了？

**A**: 因为之前安装的是 Normal 版本，后来误装了 System 版本。System 版本在 `AndroidManifest.xml` 中移除了权限声明，导致系统认为应用不需要这些权限。

### Q2: 为什么语音助手需要位置权限？

**A**: 
- **不是用来追踪您的位置！**
- Android 6.0+ 规定：获取设备MAC地址必须先有位置权限
- MAC地址用于设备识别和激活
- 我们只读取MAC地址，不会获取GPS位置

### Q3: 通知权限拒绝了，影响使用吗？

**A**: 
- **不影响核心语音功能**
- 只是无法显示服务状态通知
- 建议授予，但拒绝也能用

### Q4: System 版本什么时候用？

**A**:
- **仅用于系统预装**（出厂预装到 /system/priv-app/）
- 需要通过 EDLA 认证
- 使用系统签名
- 普通用户**不应该使用** System 版本

### Q5: 如何确保安装的是 Normal 版本？

**A**: 查看日志输出：
```
🔎 4. 查找 Normal Debug APK...
📦 APK文件: VoiceAssistant-3.19.43-Debug.apk
```

如果看到 "System"，说明找错了。

## 相关文件 📁

- `app/src/main/AndroidManifest.xml` - Normal 版本权限声明（包含所有权限）
- `app/src/system/AndroidManifest.xml` - System 版本权限声明（移除关键权限）
- `app/build.gradle.kts` - Product Flavors 配置
- `run.sh` - 构建和安装脚本
- `app/src/main/kotlin/com/ai/voice/util/PermissionHelper.kt` - 权限检查工具
- `app/src/main/kotlin/com/ai/voice/ui/floating/FloatingLauncherActivity.kt` - 权限请求逻辑

## 总结 📝

1. ✅ **关键问题**：误装了 System 版本，导致权限声明缺失
2. ✅ **核心修复**：修改 `run.sh` 明确只安装 Normal 版本
3. ✅ **优化体验**：区分关键权限和可选权限
4. ✅ **用户友好**：通知权限拒绝不影响使用

现在运行 `./run.sh` 应该能正常弹出权限请求对话框了！ 🎉


