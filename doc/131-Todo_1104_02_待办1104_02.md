注意我们要修改的asr指sensevoiceinputdevice，唤醒指的是v8wakedevice.请理解并分解以下任务，创建待办列表文档，每个待办要有明确的输入输出，实现方案和检查方案，完成文档后立即执行每项待办


1.悬浮球改为不可点击，应用启动后在后台等待唤醒不显示悬浮球，
唤醒后再显示悬浮球，悬浮球位置在屏幕左下角
，asr文本和tts文本显示修改为在悬浮球动画的水平右侧显示， 2.hyundaiit渠道不需要读取storage的权限了因为模型文件已经内置 3. 修改现有的状态管理机制，当唤醒后显示悬浮球然后asr，asr一直在后台监听，如果静音超过10后状态设置回等待唤醒并且关闭整个悬浮窗的显示，4.修改悬浮球的动画和状态的映射，未唤醒时整个悬浮窗不显示， 唤醒后悬浮球动画对应当前的idle的动画， 随后一直保持使用这个动画状态直到静音超时后退出. 5.修改asr文本识别技能的方式，支持整个asr文本中含有某些命令也能触发命令,asr文本里面可能含有多个命令，这个需求有点复杂

请为这个计划做好对应设计和分解然后实现。
