syntax = "proto3";

option java_package = "org.stypox.dicio.settings.datastore";
option java_multiple_files = true;

enum WakeDevice {
  WAKE_DEVICE_UNSET = 0;
  WAKE_DEVICE_NOTHING = 1;
  WAKE_DEVICE_OWW = 2; // openWakeWord
  WAKE_DEVICE_SHERPA_ONNX = 3; // SherpaOnnx KWS
  WAKE_DEVICE_HI_NUDGE = 4; // HiNudge Korean OpenWakeWord
}
