# 权限请求被拒绝 - 解决方案

## 问题症状

运行应用时，权限请求对话框没有弹出，设置中显示：
- "This app does not send notifications"（这个应用不会发送通知）
- 应用直接显示"权限被拒绝"，无法启动

## 问题原因

这通常是因为您之前启动过应用，并在权限请求对话框中选择了：
- **"拒绝"** 并勾选了 **"不再询问"**

一旦选择了"不再询问"，Android系统就不会再弹出权限请求对话框，权限请求会直接被拒绝。

## 快速解决方案

### 方案一：使用重置脚本（推荐）⭐

我已经为您准备了一个自动重置脚本：

```bash
./reset_permissions.sh
```

这个脚本会：
1. ✅ 检查当前权限状态
2. ✅ 清除应用数据和权限历史
3. ✅ 让您可以重新授予权限

运行后，重新启动应用（`./run.sh` 或点击应用图标），就会再次弹出权限请求对话框。

### 方案二：手动清除应用数据

如果脚本不工作，可以手动操作：

```bash
# 清除应用数据
adb shell pm clear com.ai.voice

# 重新启动应用
./run.sh
```

### 方案三：在设备设置中手动授予权限

如果您不想清除数据，可以手动在设置中授予权限：

1. **打开设备设置**
2. **进入"应用"或"应用管理"**
3. **找到"AI Voice Assistant"**
4. **点击"权限"**
5. **手动开启以下权限**：
   - ✅ **麦克风/录音** - 用于语音识别
   - ✅ **通知** - 用于显示服务状态
   - ✅ **位置** - 用于获取设备MAC地址（激活需要）
   - ✅ **显示在其他应用上层** - 用于悬浮球

### 方案四：完全重新安装

最彻底的方法：

```bash
# 1. 完全卸载应用
adb uninstall com.ai.voice

# 2. 重新安装并运行
./run.sh
```

## 新版本改进

我已经更新了代码，现在应用会智能检测权限状态：

### 1. 检测权限被永久拒绝

如果检测到权限被"不再询问"（永久拒绝），应用会显示专门的对话框：

```
┌────────────────────────────────┐
│    需要手动授予权限              │
├────────────────────────────────┤
│ 检测到您之前拒绝了以下权限：     │
│                                │
│ • 录音权限：用于语音识别和语音指令│
│ • 通知权限：用于显示语音助手服务状态│
│ • 精确位置权限：用于获取MAC地址   │
│   进行设备激活                   │
│                                │
│ 语音助手必须获得这些权限才能正常 │
│ 工作。请点击"去设置"，然后在权限 │
│ 设置中手动开启这些权限。         │
│                                │
│  [去设置]          [退出]       │
└────────────────────────────────┘
```

点击"去设置"会直接跳转到应用权限设置页面。

### 2. 首次请求权限说明

如果是首次请求权限，应用会先显示说明对话框：

```
┌────────────────────────────────┐
│         需要权限                │
├────────────────────────────────┤
│ 语音助手需要以下权限才能正常工作：│
│                                │
│ • 录音权限：用于语音识别和语音指令│
│ • 通知权限：用于显示语音助手服务状态│
│ • 精确位置权限：用于获取MAC地址   │
│   进行设备激活                   │
│                                │
│ 点击"授予权限"后，请在弹出的对话框│
│ 中选择"允许"。                   │
│                                │
│  [授予权限]        [退出]       │
└────────────────────────────────┘
```

这样用户会更清楚为什么需要这些权限。

## 权限说明

### 为什么需要这些权限？

#### 1. 📱 录音权限 (RECORD_AUDIO) - 必需
- **用途**：语音识别和语音指令
- **重要性**：没有这个权限，语音助手完全无法工作

#### 2. 🔔 通知权限 (POST_NOTIFICATIONS) - 必需
- **用途**：显示语音助手服务运行状态
- **仅 Android 13+**：Android 12 及以下版本不需要

#### 3. 📍 位置权限 (ACCESS_FINE_LOCATION) - 必需
- **用途**：获取设备MAC地址进行设备激活
- **重要性**：没有这个权限，无法完成设备激活
- **隐私说明**：
  - ⚠️ **我们不会追踪您的地理位置**
  - ⚠️ **仅用于读取MAC地址进行设备识别**
  - Android 6.0+ 要求获取MAC地址必须先获得位置权限（这是系统限制）

#### 4. 🪟 悬浮窗权限 (SYSTEM_ALERT_WINDOW) - 必需
- **用途**：显示悬浮球界面
- **重要性**：没有这个权限，无法显示悬浮球

## 测试步骤

重置权限后的完整测试流程：

```bash
# 1. 重置权限
./reset_permissions.sh

# 2. 重新启动应用
./run.sh
```

**预期行为**：

1. ✅ 应用启动
2. ✅ 显示"需要权限"说明对话框
3. ✅ 点击"授予权限"
4. ✅ 系统弹出标准的权限请求对话框（多次）
5. ✅ 依次授予：录音 → 通知（Android 13+）→ 位置
6. ✅ 显示"需要悬浮窗权限"对话框
7. ✅ 点击"前往设置"
8. ✅ 在设置中开启"显示在其他应用上层"
9. ✅ 返回应用，自动检查激活状态
10. ✅ 如果未激活，跳转到激活界面
11. ✅ 激活后，悬浮球启动

## 如果还是不行

如果尝试了以上所有方法还是不行，请：

1. **检查设备系统版本**：
   ```bash
   adb shell getprop ro.build.version.release
   ```
   确保是 Android 6.0 或更高版本

2. **检查应用是否正确安装**：
   ```bash
   adb shell pm list packages | grep com.ai.voice
   ```

3. **查看详细日志**：
   ```bash
   adb logcat | grep -E "(FloatingLauncher|PermissionHelper)"
   ```

4. **检查是否有设备管理策略限制**：
   某些企业设备或MDM管理的设备可能有权限策略限制

5. **联系技术支持**，提供以下信息：
   - 设备型号和Android版本
   - 完整的 logcat 日志
   - 截图或录屏

## 常见问答

### Q: 为什么语音助手需要位置权限？
**A**: 这不是用来追踪您的位置！Android 6.0+ 规定，应用要获取设备的MAC地址（用于设备识别和激活），必须先获得位置权限。这是Android系统的硬性要求，不是我们的设计选择。我们只会读取MAC地址，不会获取或记录您的GPS位置。

### Q: 我选择了"不再询问"后怎么办？
**A**: 运行 `./reset_permissions.sh` 清除权限历史，或者在设备设置中手动授予权限。

### Q: 能不能不授予某些权限？
**A**: 不能。所有列出的权限都是必需的，缺少任何一个权限应用都无法正常工作。

### Q: 这些权限安全吗？
**A**: 是的，所有权限都只用于应用功能，不会收集或上传您的个人信息。您可以查看源代码验证。

## 相关文件

- `app/src/main/kotlin/com/ai/voice/ui/floating/FloatingLauncherActivity.kt` - 权限请求逻辑
- `app/src/main/kotlin/com/ai/voice/util/PermissionHelper.kt` - 权限检查工具
- `reset_permissions.sh` - 权限重置脚本
- `grant_permissions.sh` - 手动授权脚本（开发用）
- `PERMISSION_FIX_SUMMARY.md` - 权限修复总结

